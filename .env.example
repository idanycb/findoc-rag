# For development, copy this file to .env and fill in the values.

# Public demo only — enables doc/user/team quotas (15 docs, 10 users, 3 teams, 5 MB uploads):
# SPRING_PROFILES_ACTIVE=demo
#
# Or run the full demo stack with Docker: make demo
# Production stack (no quotas): make prod

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=
AWS_SQS_QUEUE_NAME=
AWS_S3_BUCKET_NAME=

JWT_SECRET=

GROQ_API_KEY=

POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_URL=
POSTGRES_DB=