# Normalize text files to LF in the repository; checkout style can vary by platform.
* text=auto

# Unix scripts should always use LF.
*.sh text eol=lf
mvnw text eol=lf

# Windows command scripts should use CRLF.
*.cmd text eol=crlf
*.bat text eol=crlf
*.ps1 text eol=crlf
